import { QComponent } from '../../structure/index.js';

class PicturePageComponent extends QComponent {
  constructor(config) {
    super(config);
  }
}

export const picturePageComponent = new PicturePageComponent({
  selector: '#app-picture-page',
  template: ``,
});
